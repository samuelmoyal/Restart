# SAM (stub)

Future runway segmentation via Segment Anything (or similar promptable models).

Planned surface:

- load checkpoint
- prompt from box / point / existing rough mask
- export polygon or binary mask for `line_extraction/`
