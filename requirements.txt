# Core
huggingface_hub>=0.24
numpy>=1.24
PyYAML>=6.0
opencv-python-headless>=4.8
pyproj>=3.6
matplotlib>=3.8
Pillow>=10.0

# YOLO segmentation training / inference
ultralytics>=8.3

# PyTorch: install the CUDA build that matches your GPU, e.g.
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
torch>=2.1
